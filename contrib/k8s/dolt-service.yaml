apiVersion: v1
kind: Service
metadata:
  name: dolt
  namespace: gc
  labels:
    app: dolt
    app.kubernetes.io/part-of: gas-city
spec:
  type: ClusterIP
  selector:
    app: dolt
  ports:
  - name: mysql
    port: 3307
    targetPort: mysql
    protocol: TCP
